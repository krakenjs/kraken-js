'use strict';

var factory = confit(options);
factory
    .override('path.json')
    .override('path.json');
    .override('path.json');
    .override('path.json');
    .create(function (err, config) {
        // config.get;
        // config.set;
        // config.use;
    });
